@page
@model IndexModel
@{
    ViewData["Title"] = "Home";
}

<div class="text-center mt-5">
    <h1 class="display-4" style="font-weight: bold;">CoffeeHandler</h1>
    <p class="lead"></p>

    <!-- Tilføj en knap, der linker til login -->
    <a href="/Login" class="btn btn-primary btn-lg mt-3">Log ind</a>
</div>
 
